var assert = require('assert');

describe('Heroku API', function() {
  describe('authentication', function() {
    it('should accept username and password', function(done) {
      assert.ok(true);
      done();
    });
  });
});